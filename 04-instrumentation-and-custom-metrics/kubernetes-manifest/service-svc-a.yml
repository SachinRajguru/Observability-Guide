apiVersion: v1
kind: Service
metadata:
  name: a-service
  namespace: dev

  labels:
    app: a-service
    job: node-api

spec:
  type: LoadBalancer

  selector:
    app: service-a-deployment

  ports:
    - name: http
      port: 80
      targetPort: 3001
      protocol: TCP